---
import directus from "@/lib/directus";
import { readItems } from "@directus/sdk";

const exhibitions = await directus.request(
  readItems("exhibitions", { fields: [ "slug", ] })
);
return Astro.rewrite("/project/" + exhibitions[0].slug)
---